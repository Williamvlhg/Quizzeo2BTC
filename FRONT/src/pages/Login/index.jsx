import React from 'react';
import { CreateAccountPage } from '../../components/FormLogin'; // Assurez-vous que le chemin vers le composant est correct

const Login = () => {
    return (
        <div>
            <CreateAccountPage />
            test
        </div>
    );
}

export default Login;
